<h1>Vacations</h1>
{{#each vacations}}
  <div class="vacation">
    <h3>{{name}}</h3>
    <p>
      {{description}}
    </p>
    {{#if inSeason}}
      <span class="price">{{price}}</span>
      <!-- <a href="" class"btn btn-default">Buy now!</a> -->
    {{else}}
      <span class="outOfSeason">We're sorry, this vacation is currentely not in season.</span>
      {{! The "notify me when this vacation is in season" page will be our next task. }}
      <a href="/notify-me-when-in-season?{{sku}}">Notify me when this vacation is in season.</a>
    {{/if}}
  </div>
{{/each}}
